const defines = {};
defines.timeScale = 1;
defines.addSpeed = 240;
defines.config = {
    gameConfig: '/config/game-config'
};

defines.poolNodeType = {
    'coin': 'coin',
    'stub': 'stub',
    'enemy': 'enemy'
};

window.defines = defines;